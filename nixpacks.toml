[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "python startup.py && flask db upgrade && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
